<div dir="rtl" style="text-align: right; font-family: Tahoma, Arial, sans-serif;">
    
# الگوریتم چیست؟ — مقدمات تفکر الگوریتمی برای مهندسان هوش مصنوعی

Algorithmic Thinking for AI Engineers
## مقدمه

در دنیای مهندسی هوش مصنوعی، استفاده از مدل‌های پیچیده یادگیری ماشین و شبکه‌های عصبی تنها بخشی از ماجراست؛ زیربنای تمام این سیستم‌ها، الگوریتم‌ها هستند. بدون درک درست از الگوریتم‌ها، توانایی حل مسئله، بهینه‌سازی و تحلیل کارآمدی مدل‌ها به‌ شدت محدود می‌شود.

این درس برای ایجاد یک پایهٔ محکم در تفکر الگوریتمی طراحی شده است.

## الگوریتم چیست؟ (تعریف ساده + نگاه مهندسی)
تعریف ساده

الگوریتم یک مجموعه مراحل دقیق و قابل‌اجراست که ورودی را گرفته و خروجی مشخصی تولید می‌کند.

تعریف مهندسی

در مهندسی نرم‌افزار و هوش مصنوعی، الگوریتم عبارت است از:

یک روش سیستماتیک

با مراحل محدود و بدون ابهام

که مسئله‌ای مشخص را به حل قابل تکرار تبدیل می‌کند.

## نقش الگوریتم‌ها در هوش مصنوعی

در AI، الگوریتم‌ها در تمام مراحل حضور دارند:

جستجو و بهینه‌سازی (مثل Gradient Descent)

ساختار مدل‌ها (مثل Backpropagation، Forward Pass)

پردازش داده‌ها

طراحی معماری شبکه‌های عصبی

بهبود سرعت و کارایی سیستم‌های هوشمند

اگر الگوریتم‌ها را درک نکنید:

مدلی که می‌سازید کند می‌شود

حافظهٔ زیادی مصرف می‌کند

مقیاس‌پذیری ندارد

یا حتی درست کار نمی‌کند

## چرخه حل مسئله

این چرخه مسیر حرفه‌ای هر مهندس AI است:

    Problem → Plan → Algorithm → Code → Test → Optimize


#### 1. Problem

صورت مسئله دقیق چیست؟ ورودی/خروجی چیست؟

#### 2. Plan

چگونه می‌خواهیم مسئله را بشکنیم و راه‌حل طراحی کنیم؟

#### 3. Algorithm

مراحل را به یک فرایند مرحله به مرحله تبدیل می‌کنیم.

#### 4. Code

الگوریتم را با یک زبان مثل Python پیاده‌سازی می‌کنیم.

#### 5. Test

بررسی می‌کنیم آیا با داده‌های واقعی درست کار می‌کند؟

#### 6. Optimize

بهبود سرعت، حافظه و خوانایی.

## ویژگی‌های یک الگوریتم خوب

یک الگوریتم حرفه‌ای باید:

واضح و بدون ابهام باشد

پایان‌پذیر باشد

کارآمد باشد

قابل پیاده‌سازی با زبان‌های مختلف باشد

ورودی و خروجی مشخص داشته باشد

عمومی و قابل‌تکرار باشد

قابل تحلیل از نظر زمان و فضا باشد

## مقدمه‌ای بر پیچیدگی زمانی و فضایی

(بدون فرمول و مناسب افراد مبتدی)

پیچیدگی زمانی (Time Complexity)

معیار اینکه اجرای الگوریتم چقدر طول می‌کشد وقتی ورودی بزرگ‌تر می‌شود.

مثال:

خواندن یک عنصر → سریع

بررسی تمام عناصر یک لیست ۱ میلیون‌تایی → زمان زیاد

پیچیدگی فضایی (Space Complexity)

معیار اینکه الگوریتم چقدر حافظه مصرف می‌کند.

مثال:

ذخیره یک عدد → ناچیز

ساخت لیست ۱ میلیون عنصری → زیاد

چرا برای AI مهم است؟

زیرا مدل‌های یادگیری ماشین:

داده‌های بسیار بزرگ دارند

روی GPU/CPU محدود اجرا می‌شوند

باید سریع و بهینه باشند

## مثال الگوریتمی: پیدا کردن بزرگ‌ترین عدد در یک لیست
توضیح

می‌خواهیم از بین یک لیست اعداد، بزرگ‌ترین عدد را پیدا کنیم.

توضیح قدم به قدم

فرض کن اولین عدد لیست بزرگ‌ترین است.

یک‌به‌یک عناصر را مرور کن.

اگر عدد فعلی از بزرگ‌ترین عدد بیشتر بود، آن را جایگزین کن.

در پایان، بزرگ‌ترین مقدار به‌دست می‌آید.

</div>
#### Pseudo-Code

    Algorithm FindMax(list):
        max ← list[0]
        for each number in list:
            if number > max:
                max ← number
        return max

#### نسخه پایتون

    def find_max(arr):
        max_value = arr[0]
        for num in arr:
            if num > max_value:
                max_value = num
        return max_value

<div dir="rtl" style="text-align: right; font-family: Tahoma, Arial, sans-serif;">
    
## چرا تفکر الگوریتمی برای مهندسان AI حیاتی است؟

باعث می‌شود مسائل پیچیدهٔ هوش مصنوعی را خرد و قابل حل کنیم.

کمک می‌کند مدل‌های سریع‌تر، سبک‌تر و دقیق‌تر بسازیم.

امکان بهینه‌سازی مدل‌ها را فراهم می‌کند.

در فهم مفاهیم کلیدی مانند:

Backpropagation

Optimization Algorithms

Data Structures

Graphs

Reinforcement Learning

Search Algorithms

حیاتی است.

تفکر الگوریتمی یعنی:

«توانایی تبدیل هر مسئله به مراحل دقیق، قابل اجرا و قابل تحلیل»

این مهارتی است که یک مهندس معمولی را به یک مهندس حرفه‌ای هوش مصنوعی تبدیل می‌کند.

## تمرین‌های پیشنهادی
تمرین ۱

مسئله: مجموع اعداد یک لیست را محاسبه کنید.

ورودی: [1, 2, 3, 4]

خروجی: 10

تمرین ۲

مسئله: تعداد اعداد زوج موجود در یک لیست را پیدا کنید.

ورودی: [1, 4, 6, 7]

خروجی: 2

تمرین ۳

مسئله: بررسی کنید آیا یک لیست صعودی مرتب شده است یا خیر.

ورودی: [1, 2, 3, 5] → مرتب

ورودی: [1, 3, 2] → نامرتب

## تکلیف: نوشتن الگوریتم به صورت Pseudo-Code

برای هر سه تمرین، الگوریتم را به شکل زیر بنویسید:

</div>
    
    Algorithm Name(input):
        Step 1: ...
        Step 2: ...
        ...
        return result

_____________________________________________________________________________________________________________________________________________________________________________________________________
<div dir="rtl" style="text-align: right; font-family: Tahoma, Arial, sans-serif;">

# چرا پیچیدگی زمانی مهم است؟ 

#### روز دوم — پست آموزشی

## مقدمه

در دنیای برنامه‌نویسی، مخصوصاً وقتی وارد حوزهٔ هوش مصنوعی و علم داده می‌شویم، مسئله تنها نوشتن کد نیست؛ بلکه باید بدانیم کدی که می‌نویسیم چقدر سریع کار می‌کند و آیا هنگام کار با داده‌های بزرگ دچار کندی یا توقف می‌شود یا نه.

اینجاست که مفهوم پیچیدگی زمانی (Time Complexity) و نماد Big O وارد بازی می‌شود.

Big O به ما کمک می‌کند بدون اجرای برنامه، رفتار الگوریتم را پیش‌بینی کنیم.

### Big O چیست؟

Big O یک نماد ریاضی است که نشان می‌دهد یک الگوریتم با بزرگ‌تر شدن ورودی، چقدر کند یا سریع می‌شود.
به بیان دیگر:

Big O اندازه‌گیری می‌کند اجرای الگوریتم چطور با افزایش ورودی رشد می‌کند.

### رایج‌ترین انواع پیچیدگی زمانی
### 1️.O(1) — زمان ثابت (Constant Time)

زمان اجرا به اندازه ورودی وابسته نیست.
هرچقدر ورودی بزرگ باشد، زمان اجرا ثابت می‌ماند.

مثال:

دسترسی به یک خانه مشخص در آرایه: arr[5]

باز کردن یک کشو و برداشتن اولین وسیله

### 2️.O(n) — خطی (Linear Time)

زمان اجرا مستقیماً نسبت به تعداد ورودی‌ها افزایش می‌یابد.

مثال:

عبور از لیست برای پیدا کردن یک عدد در آرایه

گشتن دنبال یک نام در فهرست اسامی بدون ترتیب

### 3️.O(n²) — مربعی (Quadratic Time)

الگوریتم‌هایی که شامل دو حلقه تودرتو هستند.

مثال:

مقایسه هر کارت با تمام کارت‌ها برای مرتب‌سازی ساده

چیدن برگه‌ها روی میز و مقایسهٔ تک‌به‌تک آنها

### 4️.O(log n) — لگاریتمی (Logarithmic Time)

در این الگوریتم‌ها در هر مرحله، نیمی از ورودی حذف می‌شود؛ یعنی رشد بسیار کند!

مثال:

جستجوی دودویی (Binary Search)

پیدا کردن یک اسم در دفترچه تلفن مرتب با باز کردن وسط دفتر

### نمودار رشد توابع Big O

(می‌تونی این بخش را بعداً به صورت یک تصویر یا نمودار به GitHub اضافه کنی)

از سریع به کند:

O(1)  →  O(log n)  →  O(n)  →  O(n log n)  →  O(n²)  →  O(2ⁿ)  →  O(n!)


این نمودار نشان می‌دهد چرا انتخاب الگوریتم درست حیاتی است.
مثلاً O(log n) در ورودی‌های بزرگ عملکرد فوق‌العاده‌ای دارد، اما O(n²) خیلی سریع غیرقابل استفاده می‌شود.

### اهمیت Big O برای برنامه‌نویسان آیندهٔ AI

به‌عنوان کسی که می‌خواهد در هوش مصنوعی فعالیت کند، باید بدانی:

✔️ مدل‌های یادگیری ماشین با داده‌های عظیم کار می‌کنند

اگر الگوریتمت O(n²) باشد و n میلیون داده باشد، سیستم عملاً قفل می‌کند!

✔️ مرتب‌سازی، جستجو و پیمایش داده‌ها پایهٔ ML هستند

برای کار با دیتاست باید سریع تحلیل و پردازش انجام شود.

✔️ الگوریتم‌های بهینه کمک می‌کنند هزینهٔ محاسباتی کمتر شود

در پروژه‌های AI پردازنده، GPU و RAM هزینه‌بر هستند.
الگوریتم بهتر یعنی پول کمتر!

✔️ شبکه‌های عصبی خود مبتنی بر بهینه‌سازی محاسبات هستند

بخشی از سرعت این مدل‌ها از ریاضیات و بخشی از بهینه‌سازی الگوریتمی می‌آید.

📝 تمرین — سطح مبتدی تا متوسط

برای ۵ الگوریتم زیر، پیچیدگی زمانی را حدس بزن:

### 1. پیدا کردن بزرگ‌ترین عدد یک آرایه
</div>

        max_value = arr[0]
        for num in arr:
            if num > max_value:
                max_value = num

### 2. بررسی وجود یک مقدار در لیست
        
        for item in arr:
            if item == x:
                return True
        return False

### 3. جمع کردن تمام عناصر آرایه
            
            sum = 0
            for item in arr:
                sum += item

### 4. چاپ یک ماتریس n × n
        
        for i in range(n):
            for j in range(n):
                print(i, j)

### 5. جستجوی دودویی در آرایهٔ مرتب

        left, right = 0, len(arr) - 1
        while left <= right:
            mid = (left + right) // 2
            if arr[mid] == x: return True
            elif arr[mid] < x: left = mid + 1
            else: right = mid - 1


# آشنایی با الگوریتم‌های مرتب‌سازی — دید کلی قبل از کدنویسی

روز ۳ — پست آموزشی

## چرا مرتب‌سازی مهم است؟

مرتب‌سازی یکی از بنیادی‌ترین مفاهیم علوم کامپیوتر است.
تقریباً تمام الگوریتم‌های پیشرفته در یادگیری ماشین، تحلیل داده و حتی کارهای روزمرهٔ برنامه‌نویسی نیازمند داده‌ی مرتب‌شده هستند.

مرتب‌سازی اهمیت دارد چون:

پیدا کردن داده روی آرایهٔ مرتب شده چندین برابر سریع‌تر است.

بسیاری از الگوریتم‌ها مثل Binary Search فقط روی داده مرتب کار می‌کنند.

مرتب‌سازی نقش کلیدی در بهینه‌سازی عملکرد سیستم‌ها دارد.

حتی در شبکه‌های عصبی، بخش‌هایی مانند preprocessing و batching به داده‌ی ساختارمند نیاز دارند.

## آشنایی کلی با الگوریتم‌های مرتب‌سازی

در ادامه ۵ الگوریتم پایه را مرور می‌کنیم؛ هدف این است که قبل از نوشتن کد، ماهیت هر الگوریتم و موارد استفادهٔ مناسب را بشناسیم.

### 1. Bubble Sort — مرتب‌سازی حبابی

یک الگوریتم ساده که با مقایسهٔ عنصرهای کنار هم، اشتباه‌ها را یکی‌یکی به انتها "حباب" می‌دهد.

ویژگی‌ها:

ساده‌ترین برای یادگیری

عملکرد ضعیف روی داده‌های زیاد

مناسب برای آموزش، نه استفاده واقعی

زمان اجرا: O(n²)

### 2. Insertion Sort — مرتب‌سازی درجی

در این الگوریتم، هر عنصر در جای درست خود "درج" می‌شود.

ویژگی‌ها:

بسیار خوب روی داده‌های تقریباً مرتب

پیاده‌سازی آسان

کارایی مناسب برای لیست‌های کوچک

زمان اجرا:

بهترین حالت: O(n)

میانگین و بدترین: O(n²)

### 3. Selection Sort — مرتب‌سازی انتخابی

در هر مرحله کوچک‌ترین (یا بزرگ‌ترین) عنصر پیدا و در ابتدا قرار داده می‌شود.

ویژگی‌ها:

تعداد مقایسه زیاد

تعداد جابه‌جایی کم

مناسب برای موقعیت‌هایی که کاهش تعداد swap مهم است

زمان اجرا: O(n²)

### 4. Merge Sort — مرتب‌سازی ادغامی

یک الگوریتم بازگشتی که لیست را نصف کرده و بعد از مرتب کردن، دوباره ادغام می‌کند.

ویژگی‌ها:

بسیار سریع و پایدار

مناسب داده‌های بزرگ

نیاز به فضای اضافی دارد

زمان اجرا:

همیشه: O(n log n)

### 5. Quick Sort — مرتب‌سازی سریع

عنصر محوری (pivot) انتخاب شده و داده‌ها به دو بخش تقسیم می‌شوند.

ویژگی‌ها:

یکی از پرکاربردترین الگوریتم‌ها

عملکرد عالی در بیشتر موارد

بدترین حالت با انتخاب بد pivot بسیار کند می‌شود

زمان اجرا:

میانگین: O(n log n)

بدترین: O(n²)

## تفاوت in-place و not in-place
 #### in-place

الگوریتم داده را در همان فضای اصلی مرتب می‌کند.
نیازی به آرایهٔ کمکی ندارد یا بسیار کم استفاده می‌کند.

مثال:

Bubble Sort

Insertion Sort

Quick Sort (تقریباً)

#### not in-place

برای مرتب‌سازی نیاز به فضای اضافی دارد.

مثال:

Merge Sort

## تفاوت stable و unstable sorting
#### stable

عنصرهای برابر، ترتیب اولیهٔ خود را حفظ می‌کنند.
زمانی که داده چند ویژگی دارد بسیار مهم است.

مثال:

Merge Sort

Insertion Sort

🔵 unstable

ترتیب اولیهٔ عناصر برابر ممکن است تغییر کند.

مثال:

Quick Sort

Selection Sort

📝 تمرین امروز
✳ سؤال:

هر الگوریتم چه زمانی بهتر است استفاده شود؟

برای خودت جواب بده:

داده‌ات کوچک است یا بزرگ؟

تقریباً مرتب است یا کاملاً نامرتب؟

فضای اضافه داری یا نه؟

نیاز به پایداری (stable) داری؟

سرعت مهم‌تر است یا سادگی پیاده‌سازی؟

اگر خواستی، تمرینت را برایم بفرست تا نقد و اصلاحش کنم

در اکانت گیت هابم شروع به اموزش کردم و امروز کلیتی از  پست سوم را برای شما اپلود میکنم .
«اولین قدم‌های عملی در پیاده‌سازی الگوریتم‌ها — از تئوری تا کدنویسی»


---

 مقدمه

تا اینجا دربارهٔ Big O و مفهوم کلی الگوریتم‌های مرتب‌سازی صحبت کردیم.
امروز وقتشه وارد مرحله‌ی عملی بشیم؛ جایی که تفاوت یک توسعه‌دهنده‌ی معمولی با یک مهندس سیستم‌های هوشمند کاملاً مشخص میشه:

 تبدیل مفهوم به کد
 انتخاب پیاده‌سازی مناسب
درک جزئیات رفتاری الگوریتم


---

 امروز دقیقاً چه چیزی را یاد می‌گیریم؟

تبدیل تفکر الگوریتمی به کد

خیلی‌ها مشکل‌شان در کدنویسی این نیست که زبان بلد نیستند؛
مشکل این است که ذهن‌شان الگوریتمی فکر نمی‌کند.

امروز تمرکز روی این سه مهارت است:

تشخیص ورودی و خروجی

شکستن مسئله به مراحل کوچک

انتخاب ساختار دادهٔ مناسب



---

 ساختار کلی نوشتن یک الگوریتم

برای هر الگوریتمی که می‌نویسی بهتر است همیشه این قالب ثابت را رعایت کنی:

مرحله ۱: تعریف مسئله

دقیق بدانیم الگوریتم چه چیزی را قرار است حل کند.

مرحله ۲: مشخص کردن ورودی/خروجی

این کار باعث می‌شود سردرگمی در منطق ایجاد نشود.

 مرحله ۳: طراحی قدم‌به‌قدم (Step-by-Step Logic)

اینجا از تفکر الگوریتمی استفاده می‌کنیم.

 مرحله ۴: پیاده‌سازی پایتون

تبدیل مراحل به کد تمیز، قابل فهم و تست‌پذیر.

مرحله ۵: تحلیل زمانی (Big O)

برای اینکه بدانیم الگوریتم‌مان چقدر مقیاس‌پذیر است.


---

 ۳ الگوریتم ساده برای شروع (با توضیح و نگاه مهندسی)

 1. پیدا کردن کوچک‌ترین عدد در لیست

این یکی از پایه‌ای‌ترین الگوریتم‌هاست.

چرا مهم است؟
پایهٔ Selection Sort و بسیاری از الگوریتم‌های دیگر است.

نگاه الگوریتمی:

فرض کن عدد اول کوچک‌ترین است

کل لیست را پیمایش کن

اگر عدد کوچک‌تری دیدی جایگزین کن


Big O:
O(n)


---

 2. محاسبهٔ مجموع لیست

یک الگوریتم بسیار ساده اما ضروری برای درک پیمایش.

چرا مهم است؟
در تجزیه‌وتحلیل داده، عملیات جمع یکی از پایه‌هاست.

Big O:
O(n)


---

⭐ 3. شمارش تعداد تکرار یک مقدار

برای بسیاری از مسائل آماری اولیه مهم است.

مثال واقعی:
چندبار مقدار 0 در دیتا وجود دارد؟
چندبار کلاس "cat" در مجموعهٔ تصاویر دیده می‌شود؟

Big O:
O(n)


---

 نکتهٔ مهم امروز

تا وقتی نتوانی الگوریتم‌های ساده را با دقت و درک کامل بنویسی،
سراغ الگوریتم‌های پیچیده‌تر نرو.

این دقیقاً راه همان مهندسانی است که:

کد تمیز دارند

منطق شفاف دارند

و در پروژه‌های بزرگ دچار سردرگمی نمی‌شوند



---

 تمرین امروز

✳ تمرین:

برای هر سه الگوریتم بالا، مراحل زیر را انجام بده:

1. مسئله را خودت تعریف کن


2. ورودی/خروجی را مشخص کن


جست‌وجو چیست؟ — آشنایی با Search در الگوریتم‌ها

یکی از مهم‌ترین کارهایی که هر برنامه باید انجام دهد، پیدا کردن یک مقدار در میان مجموعه‌ای از داده‌هاست.
به این کار می‌گوییم جست‌وجو (Search).


---

🔍 جست‌وجو یعنی چه؟

جست‌وجو یعنی بررسی یک لیست یا مجموعه داده برای اینکه ببینیم آیا مقدار موردنظر وجود دارد یا نه.

مثال سادهٔ روزمره

فرض کن دنبال اسم «علی» در فهرست مخاطبین گوشی می‌گردی:

اگر لیست نامرتب باشد → باید یکی‌یکی چک کنی → جست‌وجوی خطی (Linear Search)

اگر لیست مرتب باشد → از وسط شروع می‌کنی تا سریع‌تر پیدا شود → جست‌وجوی دوگانه (Binary Search)



---

⚡ دو روش مهم جست‌وجو

1️⃣ جست‌وجوی خطی (Linear Search)

در این روش از اول لیست تا آخر جلو می‌رویم و هر مقدار را چک می‌کنیم.

مزایا: ساده، بدون نیاز به لیست مرتب
محدودیت: کند؛ مخصوصاً برای لیست‌های بزرگ


---

2️⃣ جست‌وجوی دوگانه (Binary Search)

در این روش لیست باید مرتب باشد.
از وسط شروع می‌کنیم:


اگر مقدار وسط بزرگ‌تر بود → نیمه چپ

اگر کوچک‌تر بود → نیمه راست

اگر برابر بود → پیدا شد!


مزایا: بسیار سریع، مخصوص لیست‌های بزرگ
محدودیت: فقط برای لیست‌های مرتب


---

🔹 شبه‌کد Linear Search

برای هر عنصر در لیست:
    اگر عنصر برابر مقدار موردنظر بود:
        مقدار پیدا شد
اگر تا آخر لیست نرسیدیم:
    مقدار یافت نشد


---

✔️ تمرین روز ۱

یک لیست ۱۰تایی مثل مثال زیر بساز:


     [8, 3, 15, 1, 9, 22, 7, 12, 5, 18]


۱. یک عدد انتخاب کن (مثلاً 12)
۲. با روش جست‌وجوی خطی جلو برو
۳. مرحله‌به‌مرحله بنویس که کدام عناصر را چک کردی تا به جواب رسیدی

اگر خواستی می‌تونم همین تمرین را برایت حل‌شده هم قرار بدهم.

